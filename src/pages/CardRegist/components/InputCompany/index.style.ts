import styled from '@emotion/styled';

export const Container = styled.div`
  width: 100%;
`;

export const InputCompanyContainer = styled.div`
  position: absolute;
  bottom: 0px;
  left: 0px;

  width: 100%;
  background-color: ${({ theme }) => theme.color.white};
`;
